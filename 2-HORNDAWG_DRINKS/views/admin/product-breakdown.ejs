<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Horn Dawg Drinks Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <!-- Navigation -->
    <%- include('../partials/admin-nav') %>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-8">Product Breakdown</h1>

        <!-- Product Price Info -->
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">
            <div class="flex items-center gap-3">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <div>
                    <h3 class="font-semibold text-blue-900">Current Product Price</h3>
                    <p class="text-blue-700">€<%= productPrice ? parseFloat(productPrice).toFixed(2) : '24.99' %> per tray</p>
                </div>
            </div>
        </div>

        <!-- Product Statistics Table -->
        <div class="bg-white rounded-lg shadow overflow-hidden">
            <% if (breakdown && breakdown.length > 0) { %>
                <div class="overflow-x-auto">
                    <table class="min-w-full">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Product Name</th>
                                <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total Orders</th>
                                <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total Units Sold</th>
                                <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total Revenue</th>
                                <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Avg Order Value</th>
                                <th class="px-6 py-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Market Share</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-200">
                            <% 
                            const totalOrders = breakdown.reduce((sum, p) => sum + parseInt(p.total_orders), 0);
                            const totalRevenue = breakdown.reduce((sum, p) => sum + parseFloat(p.total_revenue), 0);
                            %>
                            <% breakdown.forEach(product => { %>
                                <tr class="hover:bg-gray-50">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <div class="h-10 w-10 flex-shrink-0 rounded-full flex items-center justify-center
                                                <%= product.product.includes('Watermelon') ? 'bg-red-100' : 'bg-blue-100' %>">
                                                <span class="text-lg">
                                                    <%= product.product.includes('Watermelon') ? '🍉' : '🫐' %>
                                                </span>
                                            </div>
                                            <div class="ml-4">
                                                <div class="text-sm font-medium text-gray-900"><%= product.product %></div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="text-sm font-semibold text-gray-900"><%= product.total_orders %></div>
                                        <div class="text-xs text-gray-500">
                                            <%= ((parseInt(product.total_orders) / totalOrders) * 100).toFixed(1) %>% of total
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                        <%= product.total_units %> units
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="text-sm font-bold text-green-600">€<%= parseFloat(product.total_revenue).toFixed(2) %></div>
                                        <div class="text-xs text-gray-500">
                                            <%= ((parseFloat(product.total_revenue) / totalRevenue) * 100).toFixed(1) %>% of total
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                        €<%= parseFloat(product.avg_order_value).toFixed(2) %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="w-full bg-gray-200 rounded-full h-2">
                                            <div class="bg-blue-600 h-2 rounded-full" 
                                                 style="width: <%= ((parseInt(product.total_orders) / totalOrders) * 100).toFixed(1) %>%"></div>
                                        </div>
                                        <span class="text-xs text-gray-500 mt-1">
                                            <%= ((parseInt(product.total_orders) / totalOrders) * 100).toFixed(1) %>%
                                        </span>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                        <tfoot class="bg-gray-50">
                            <tr class="font-bold">
                                <td class="px-6 py-4 text-sm text-gray-900">TOTAL</td>
                                <td class="px-6 py-4 text-sm text-gray-900"><%= totalOrders %></td>
                                <td class="px-6 py-4 text-sm text-gray-900">
                                    <%= breakdown.reduce((sum, p) => sum + parseInt(p.total_units), 0) %> units
                                </td>
                                <td class="px-6 py-4 text-sm text-green-600">€<%= totalRevenue.toFixed(2) %></td>
                                <td class="px-6 py-4 text-sm text-gray-900">€<%= (totalRevenue / totalOrders).toFixed(2) %></td>
                                <td class="px-6 py-4"></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            <% } else { %>
                <div class="text-center py-12">
                    <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                    </svg>
                    <h3 class="mt-2 text-sm font-medium text-gray-900">No product data available</h3>
                    <p class="mt-1 text-sm text-gray-500">Product statistics will appear here once orders are placed.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>